package views

import (
	"github.com/nbittich/factsfood/views/layout"
	"github.com/nbittich/factsfood/views/utils"
)

templ Home(name string) {
	@layout.Layout("home.title", "/", home(name), nil, nil)
}

templ home(name string) {
	<div>{ utils.T(ctx,"home.hello") } { utils.T(ctx, name) }!</div>
}
